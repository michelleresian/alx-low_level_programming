#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @x: arguments for add
 * @y: arguments for add
 * Return: result
 */
int add(int x, int y)
{
	return (x + y);
}
